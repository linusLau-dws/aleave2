package hk.com.dataworld.leaveapp.helper;

//import org.apache.commons.net.ntp.NTPUDPClient;
//import org.apache.commons.net.ntp.TimeInfo;

import java.io.IOException;
import java.net.InetAddress;
import java.util.Date;

public final class GetServerTime {

//    private static final String TAG = GetServerTime.class.getSimpleName();

//    //NTP server list: http://tf.nist.gov/tf-cgi/servers.cgi
//    private static final String TIME_SERVER = "hk.pool.ntp.org";
//
//    public static Date getCurrentNetworkTime() throws IOException {
//        NTPUDPClient timeClient = new NTPUDPClient();
//        InetAddress inetAddress = InetAddress.getByName(TIME_SERVER);
//        TimeInfo timeInfo = timeClient.getTime(inetAddress);
//        long returnTimeDevice = timeInfo.getReturnTime();   //local device time
//        long returnTimeServer = timeInfo.getMessage().getTransmitTimeStamp().getTime();   //server time
//        Date timeServer = new Date(returnTimeServer);
//        return timeServer;
//    }
}